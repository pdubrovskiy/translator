import classes from "./Header.module.css";

const Header = (props) => {
    return(
        <header>
        <div>Translator</div>
        <div>About</div>
        </header>
    )
}

export default Header;